import 'mocha'


describe('MythChannelController',()=>{
    context('directives',()=>{
        it('should ChangeChannel')
        it('should SkipChannels')
    })
    context('myth events',()=>{
        it('should PLAY_CHANGED')
        it('should LIVETV_STARTED')
        it('should LIVETV_ENDED')
        it('should PLAY_STOPPED')
    })
    context('shadow',()=>{
        it('should refreshState')
        it('should refreshCapability')
    })
})